% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep_card.R
\name{prep_card}
\alias{prep_card}
\title{Prepare card for tfrmt}
\usage{
prep_card(
  x,
  column = NULL,
  group = NULL,
  variables = NULL,
  fill_overall = "Overall {colname}",
  fill = "Any {colname}",
  fill_from = NULL
)
}
\arguments{
\item{x}{an ARD data frame of class 'card'}

\item{column}{(character) variable(s) to use for column names.}

\item{group}{(character) grouping variables to use in the formatted table.}

\item{variables}{(character) incoming \code{cards} variables.}

\item{fill_overall}{scalar to fill missing grouping or variable levels. If a
character is passed, then it is processed with \code{\link[glue:glue]{glue::glue()}} where the
colname element is available to inject into the string,
e.g. \verb{Overall \{colname\}} may resolve to \code{"Overall AGE"} for an \code{AGE}
column. Default is \verb{Overall \{colname\}}. If \code{NA} then no fill will occur.}

\item{fill}{(character) value to fill with. Defaults to \code{"Any {colname}"}.
Not used if \code{fill_from} is specified.}

\item{fill_from}{(character) Indicates when doing pair-wise filling whether
to fill from the column to the left. Defaults to \code{NULL}. Can be either \code{NULL}
or \code{"left"}.}
}
\value{
a \code{data.frame}
}
\description{
What does the preparation function do?
\itemize{
\item \code{prep_unite_vars()}: brings all categorical variables levels into a single
column, called \code{variable_level} (where applicable).
\item \code{prep_big_n()}: recodes the \code{"n"} \code{stat_name} into \code{"bigN"} for the
desired columns.
\item \code{prep_label()}: creates a \code{label} column from \code{variable_level} for
categorical variables and \code{stat_label} for all other variable types.
\item \code{prep_fill_pairwise()}: in a hierarchical stack replaces NA in one column
conditional on the presence of data in another column.
}
}
